numba main() {
    numba i = 10;
    numba j = 14;

    iz b = i > j;

    despite (b || i > 9) {
        showMeNumba(420);
    }
    unless {
        showMeNumba(69);
    }

    despite (b || j > 100) {
        showMeNumba(69);
    } unless {
        showMeNumba(420);
    }

    gimme i;
}