
numba main() {
    E numba i = 25;
    showMeNumba(i);

    iz b = tru;
    despite (!(tru) || !trunt ||b) {
        showMeNumba(69);
    } unless {
        showMeNumba(420);
    }

    numba num = 10;
    gimme num + 5;
}
