
shrek test {
    numba x;
    numba y;
};

numba main() {

    numba a;
    numba b;
    numba c;
    numba d;
    shrek test t;

    c = 5;

    a = b = ((c == 5) ? 10 : 0);

    showMeNumba(a + b + c);

    a = t.y = d = b = c = (a = 100) + 5;

    showMeNumba(a);
    showMeNumba(d);
    showMeNumba(t.y);

    gimme a;
}
